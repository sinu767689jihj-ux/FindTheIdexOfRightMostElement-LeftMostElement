class Solution{
    public int leftMostElement(int[] nums,int target){
        int l=0,r=nums.length-1;
        int mid=0;
        while(l<=r){
            mid=l+(r-l)/2;
            if(nums[mid]==target){
                r=mid-1;
            }else if(nums[mid]>target){
                r=mid-1;
            }else{
                l=mid+1;
            }
        }
        if(l>=nums.length){
        return -1;
        }
        if(nums[l]!=target){
            return -1;
        }
        return l;
    }
    public static void main(String[] args){
        int[] nums={1,2,2,2,2,3};
        int target=2;
        Solution s=new Solution();
        System.out.println(s.leftMostElement(nums,target));
    }
}
